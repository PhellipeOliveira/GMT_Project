# Web API
fastapi==0.116.1
uvicorn[standard]==0.35.0
python-dotenv==1.1.1
python-multipart==0.0.20

# Agente (LangChain / LangGraph)
langchain==0.3.27
langchain-openai==0.3.33
langchain-text-splitters==0.3.9
langchain-core==0.3.79
langgraph==0.6.7
langgraph-prebuilt==0.6.4


# Banco (Supabase / Postgres)
psycopg[binary]==3.2.10
psycopg-pool==3.2.6

# E-mail (confirmações ao lead + alertas à equipe)
resend==2.32.2

# Google Calendar (push one-way na confirmação de reunião)
google-api-python-client==2.169.0
google-auth==2.40.3

# Persistência do agente (checkpointing no Supabase/Postgres)
langgraph-checkpoint-postgres==2.0.23

# Dev: LangGraph Studio (`make studio`) + testes (`make test`)
langgraph-cli==0.4.2
pytest==8.4.2

# Opcional (rerank Cohere no RAG): langchain-cohere + COHERE_API_KEY
